# DAB 音乐播放器 TODO 列表

## 🔴 关键问题 (必须修复)

### 1. 配置系统未实现
**状态**: 未完成  
**优先级**: 最高  
**问题**: `Config::load()` 只是 TODO 占位符，总是返回默认配置
```rust
// src/config.rs:49 - 需要实现
pub fn load() -> Config {
    // TODO: Load config from file
    println!("TODO: Load config from file, using default");
    Config::default()
}
```
**影响**: 用户无法永久保存设置，每次重启都会丢失偏好
**解决方案**: 实现配置文件读取、创建默认配置文件、错误处理

### 2. UI 在库操作时阻塞
**状态**: 已识别 (见 library_fix_proposal.md)  
**优先级**: 最高  
**问题**: `scan_cached_files()` 中的同步 I/O 会冻结 UI
**影响**: 大型音乐库的用户体验很差
**解决方案**: 
- 使用 `spawn_blocking` 处理 ID3 标签读取
- 实现异步文件读写操作
- 批量处理避免长时间阻塞

### 3. 缺少错误恢复机制
**状态**: 未完成  
**优先级**: 高  
**问题**: 基础错误类型存在但恢复策略有限
**影响**: 播放失败后无恢复选项
**解决方案**: 
```rust
// 添加自动降级策略
if streaming_fails_count > 3 {
    fallback_to_full_download().await?;
}
// 添加重试机制与指数退避
```

## 🟡 主要缺失功能

### 4. 无播放列表支持
**状态**: 未完成  
**优先级**: 高  
**问题**: 只有收藏夹（单个专辑列表）
**需求**: 
- 自定义播放列表创建/管理
- 智能播放列表（最近播放、最常播放）
- 播放列表导入/导出
- 播放列表视图和管理 UI

### 5. 库功能受限
**状态**: 部分完成  
**优先级**: 高  
**问题**: 
- 只扫描缓存文件，不扫描本地音乐目录
- `scan_local_files()` 是 TODO 实现
- 没有库搜索/过滤功能
- 无法按流派、年份或自定义标签组织

### 6. 音频功能未实现
**状态**: 设置存在但功能未实现  
**优先级**: 中  
**缺失功能**:
- 交叉淡入淡出 (设置在 config 中但未使用)
- 无缝播放
- 均衡器
- 音量标准化
- 播放速度控制
- 音频可视化 (cpal 依赖暗示计划但未实现)
- A-B 重复
- 位置跳转功能不完整

### 7. TUI 功能限制
**状态**: 基础功能完成  
**优先级**: 中  
**当前视图**: Library, AlbumDetail, Queue, Search, DetailedAlbum, ArtistDiscography (共6个)
**缺失视图**:
- 设置/偏好视图
- 播放列表管理视图
- 歌词显示视图
- 可视化视图
**其他限制**:
- 键盘快捷键有限
- 未启用鼠标支持 (尽管 crossterm 支持)

## 🟢 性能优化需求

### 8. 架构改进
**状态**: 需要优化  
**优先级**: 中  
**建议**:
- UI 线程与播放线程完全分离
- 考虑使用专门的解码线程池
- 让 PlayerEngine 运行在独立的 runtime 上
- UI 只通过 channel 接收播放状态，不直接调用播放逻辑
- 添加性能监控指标

### 9. 流式播放增强
**状态**: 基础实现完成，需要增强  
**优先级**: 中  
**建议改进**:
```rust
// 在 StreamingAudioSource 中增加预读缓冲
pub async fn ensure_minimum_buffer_for_playback(&self) -> bool {
    let available_ms = self.get_available_playback_ms().await;
    let config = self.adaptive_config.read().await;
    available_ms >= config.min_playback_buffer_ms
}
```
- 基于网络条件的自适应比特率
- 缓冲区健康监控
- 更智能的预加载策略

### 10. 网络和缓存优化
**状态**: 基础功能完成  
**优先级**: 低  
**改进点**:
- 流 URL 缓存自动刷新
- 离线模式处理
- 预加载下一曲目功能 (设置存在但未使用)

## 📊 生活质量功能

### 11. 标准播放器功能
**状态**: 未完成  
**优先级**: 低  
**缺失功能**:
- A-B 重复
- 睡眠定时器
- 书签位置
- 更完整的跳转功能

### 12. 测试覆盖率改进
**状态**: 基础测试存在 (12个测试文件)  
**优先级**: 低  
**需要补充**:
- UI 测试
- 完整播放场景的集成测试
- 错误恢复路径测试

## 🔧 代码质量问题

### 13. TODO 和未完成实现
**状态**: 需要清理  
**优先级**: 低  
**问题**:
- 代码中多个 TODO 注释
- 一些功能有设置但无实现
- Cargo.toml 中的 "# Missing dependencies" 注释 (但依赖项已包含)

### 14. 文档改进
**状态**: 基础文档存在  
**优先级**: 低  
**改进点**:
- CLAUDE.md 可以记录更多架构决策
- 除了 TUI_SHORTCUTS.md 外缺少用户文档
- API 集成文档完善但实现细节稀少

## 🎯 推荐优先级顺序

1. **最高优先级**: 修复 UI 阻塞、实现配置持久化、添加错误恢复
2. **高优先级**: 添加播放列表支持、完善库扫描、实现基本音频功能
3. **中优先级**: TUI 功能扩展、性能优化、流式播放增强
4. **低优先级**: 可视化、鼠标支持、高级功能

## 💡 用户体验优化建议

- 在加载在线音频时显示缓冲进度
- 网络状况差时自动提示用户
- 支持手动切换"快速响应"vs"稳定播放"模式
- 添加关键指标监控：
  - UI 事件响应时间
  - 音频缓冲区健康度
  - 网络下载速度变化

---

**总结**: 项目有坚实的基础架构，但需要大量工作才能成为功能完整的音乐播放器。架构设计良好，但许多计划的功能仍未实现。建议优先解决关键的用户体验问题，然后逐步添加标准音乐播放器功能。