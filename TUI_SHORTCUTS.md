# DAB Music Player - TUI 快捷键

## 全局快捷键

| 按键 | 功能 | 说明 |
|------|------|------|
| `q` | 退出程序 | 退出 DAB 音乐播放器 |
| `1` | 切换到音乐库视图 | 显示本地音乐库 |
| `2` | 切换到播放队列视图 | 显示当前播放队列 |
| `3` | 切换到搜索视图 | 显示搜索界面 |
| `/` | 进入搜索模式 | 开始搜索音乐 |
| `Space` | 播放/暂停 | 切换播放状态 |
| `n` | 下一首 | 播放下一首歌曲 |
| `p` | 上一首 | 播放上一首歌曲 |
| `s` | 停止 | 停止当前播放 |
| `Up/Down` | 导航 | 在列表中上下移动 |
| `Enter` | 选择/确认 | 选择项目或确认操作 |
| `Esc` | 返回/取消 | 返回上一级或取消操作 |

## 视图特定快捷键

### 音乐库视图 (Library)
| 按键 | 功能 | 说明 |
|------|------|------|
| `l` | 进入专辑详情 | 显示选中专辑的详细信息 |
| `Enter` | 进入专辑详情 | 同 `l` 键功能 |

### 专辑详情视图 (Album Detail)
| 按键 | 功能 | 说明 |
|------|------|------|
| `a` | 添加到队列下一首 | 将当前选中的歌曲添加到播放队列的下一个位置 |
| `A` | 清空队列并播放所有 | 清空当前播放队列，将专辑中所有歌曲添加到队列并开始播放 |
| `Enter` | 播放选中歌曲 | 立即播放当前选中的歌曲 |

### 搜索视图 (Search)
| 按键 | 功能 | 说明 |
|------|------|------|
| `l` | 查看专辑详情 | 显示选中歌曲/专辑的详细信息和曲目列表 |
| `h` | 查看艺术家信息 | 显示选中歌曲艺术家的介绍信息和专辑列表 |
| `a` | 添加到队列下一首 | 将当前选中的歌曲添加到播放队列的下一个位置 |
| `A` | 清空队列并播放所有 | 清空当前播放队列，将搜索结果中的所有歌曲添加到队列并开始播放 |
| `Tab` | 切换搜索类型 | 在歌曲、专辑、艺术家之间切换搜索类型 |
| `Enter` | 执行搜索/播放歌曲 | 在搜索模式下执行搜索，在结果列表中播放选中歌曲 |
| `Backspace` | 删除字符 | 在搜索模式下删除搜索词 |
| `Esc` | 退出搜索模式 | 退出搜索模式返回搜索结果 |

### 专辑详情视图 (Detailed Album)
| 按键 | 功能 | 说明 |
|------|------|------|
| `h` | 查看艺术家信息 | 显示专辑艺术家的介绍信息和专辑列表 |
| `a` | 添加到队列下一首 | 将当前选中的歌曲添加到播放队列的下一个位置 |
| `A` | 清空队列并播放所有 | 清空当前播放队列，将专辑中所有歌曲添加到队列并开始播放 |
| `Enter` | 播放选中歌曲 | 立即播放当前选中的歌曲 |
| `Esc` | 返回搜索视图 | 返回到搜索视图 |

### 艺术家专辑列表视图 (Artist Discography)
| 按键 | 功能 | 说明 |
|------|------|------|
| `l` | 查看专辑详情 | 显示选中专辑的详细信息和曲目列表 |
| `Enter` | 查看专辑详情 | 同 `l` 键功能 |
| `Esc` | 返回搜索视图 | 返回到搜索视图 |

## 搜索模式
在搜索模式下，可以输入搜索词，然后按 `Enter` 执行搜索。

## 状态指示器
- `[cached]` - 表示该歌曲已缓存到本地
- `►` - 表示当前选中的项目
- 搜索结果格式：`艺术家 - 专辑 - 歌曲名`

## 颜色编码
- **白色** - 歌曲名
- **青色** - 艺术家
- **蓝色** - 专辑
- **黄色** - 其他信息
- **绿色** - 缓存指示器

## 使用提示
1. 在音乐库视图中，使用 `l` 或 `Enter` 查看专辑详情
2. 在专辑详情视图中，使用 `a` 快速添加歌曲到下一首播放
3. 使用 `A` 可以一次性播放整个专辑
4. 在搜索结果中，使用 `l` 查看专辑详情，`h` 查看艺术家信息
5. 搜索结果会显示艺术家-专辑-歌曲的格式，方便识别
6. 已缓存的歌曲会显示 `[cached]` 标识
7. 使用 `Tab` 键可以在搜索时切换搜索类型（歌曲/专辑/艺术家）
8. 使用 `Esc` 键可以返回上一级视图

## 注意事项
- 按键 `a` 和 `A` 在所有歌曲列表界面中有效：专辑详情视图、详细专辑视图、搜索视图
- 按键 `l` 在音乐库视图、搜索视图和艺术家专辑列表视图中有效
- 按键 `h` 在搜索视图和详细专辑视图中有效
- 在艺术家专辑列表视图中，按键 `A` 会提示先选择专辑，因为直接播放所有专辑可能包含过多歌曲
- 搜索功能支持在线音乐搜索，需要网络连接
- 缓存功能会自动管理本地存储空间
- 专辑详情和艺术家信息需要从在线API获取
- 使用 `A` 键添加多首歌曲时会显示加载进度信息

## 命令行使用

DAB 音乐播放器也支持命令行操作：

```bash
# 播放音乐
dab play [track_id_or_url]

# 暂停播放
dab pause

# 恢复播放
dab resume

# 停止播放
dab stop

# 下一首
dab next

# 上一首
dab prev

# 添加到队列
dab queue <url>

# 搜索音乐
dab search <query>

# 搜索特定类型的内容
dab search <query> --type track    # 搜索歌曲（默认）
dab search <query> --type album    # 搜索专辑
dab search <query> --type artist   # 搜索艺术家

# 搜索并限制结果数量
dab search <query> --limit 10

# 显示状态
dab status

# 启动 TUI 界面
dab tui
```

### 搜索选项说明

- `--type` 或 `-t`: 指定搜索类型，支持 `track`（歌曲）、`album`（专辑）、`artist`（艺术家），默认为 `track`
- `--limit` 或 `-l`: 指定返回结果数量，范围 1-50，默认为 20

如果没有指定命令，默认启动 TUI 界面。